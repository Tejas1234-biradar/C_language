#include <stdio.h>

int main(){
    char st[30];
    // scanf("%s ",st);
    gets(st);
    puts(st);//taks cursor to next line in summary has an internal \n
    //a strig defined using popinters can be redifinded
    return 0;
}